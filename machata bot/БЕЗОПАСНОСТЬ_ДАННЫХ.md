# 💾 Безопасность и сохранение данных

## 📁 Файлы с данными

Бот автоматически сохраняет все данные в следующие файлы:

1. **`machata_bookings.json`** — все бронирования клиентов
2. **`vip_users.json`** — список VIP клиентов с индивидуальными ценами
3. **`machata_config.json`** — конфигурация (цены, расписание и т.д.)

## ✅ Автоматическое сохранение

Все данные сохраняются **автоматически**:
- ✅ При каждом новом бронировании
- ✅ При изменении статуса брони
- ✅ При добавлении/удалении VIP клиента
- ✅ При изменении индивидуальной цены для VIP

**ВАЖНО:** Эти файлы уже добавлены в `.gitignore`, поэтому они **НЕ попадут в Git** и не будут потеряны при коммитах.

## 🔒 Безопасность

Все файлы с данными исключены из Git:
```
machata_bookings.json
vip_users.json
machata_config.json
```

Это защищает:
- ✅ Личные данные клиентов
- ✅ Историю бронирований
- ✅ VIP клиентов от попадания в публичный репозиторий

## 💾 Резервное копирование

### Перед обновлением кода:

1. **Создайте резервную копию файлов:**
   ```bash
   cp machata_bookings.json machata_bookings.json.backup
   cp vip_users.json vip_users.json.backup
   cp machata_config.json machata_config.json.backup
   ```

2. **Или создайте папку для бэкапов:**
   ```bash
   mkdir backups
   cp *.json backups/
   ```

### Рекомендуется делать резервные копии:
- Перед каждым обновлением кода
- Раз в день (можно автоматизировать)
- Перед деплоем на сервер

## 🚀 При деплое на Railway/Render

### Важно понимать:

1. **Локальные файлы НЕ загружаются на сервер** (они в .gitignore)
2. **На сервере создаются НОВЫЕ пустые файлы** при первом запуске
3. **Данные с сервера НЕ синхронизируются с локальными**

### Варианты решения:

#### Вариант 1: Хранить данные только локально
- Запускайте бота только локально
- Файлы останутся на вашем компьютере

#### Вариант 2: Перенести данные на сервер (вручную)
1. Создайте файлы на сервере через SSH или веб-интерфейс
2. Скопируйте содержимое локальных файлов
3. Вставьте в файлы на сервере

#### Вариант 3: Использовать базу данных (рекомендуется для продакшена)
- PostgreSQL на Railway
- MongoDB
- SQLite (проще для начала)

## 📋 Что делать перед обновлением кода

1. **Остановите бота** (Ctrl+C)
2. **Сделайте резервную копию:**
   ```bash
   cp machata_bookings.json machata_bookings.json.backup
   cp vip_users.json vip_users.json.backup
   ```
3. **Обновите код**
4. **Запустите бота заново**
5. **Проверьте, что данные на месте**

## 🔍 Проверка данных

Чтобы проверить, что данные сохраняются:

```bash
# Посмотреть количество броней
cat machata_bookings.json | grep -c '"id"'

# Посмотреть VIP клиентов
cat vip_users.json
```

## ⚠️ ВАЖНО

- **НЕ коммитьте файлы с данными в Git** (они уже в .gitignore)
- **НЕ удаляйте файлы** `*.json` без резервной копии
- **Делайте бэкапы регулярно**, особенно перед обновлениями
- **На сервере данные хранятся отдельно** от локальных файлов

## 💡 Совет

Для автоматического бэкапа создайте скрипт:

```bash
#!/bin/bash
# backup.sh
DATE=$(date +%Y%m%d_%H%M%S)
mkdir -p backups
cp machata_bookings.json backups/bookings_$DATE.json
cp vip_users.json backups/vip_$DATE.json
cp machata_config.json backups/config_$DATE.json
echo "Бэкап создан: backups/*_$DATE.json"
```

Запускайте перед каждым обновлением!
