# 🚀 Инструкция по деплою бота

## ✅ Что было исправлено

1. **Создан `requirements.txt`** - файл с зависимостями для деплоя
2. **Создан `.gitignore`** - чтобы не коммитить секретные данные
3. **Создан `Procfile`** - для деплоя на Render/Heroku

## 📋 Необходимые файлы для деплоя

- ✅ `requirements.txt` - зависимости Python
- ✅ `Procfile` - команда запуска для Render/Heroku
- ✅ `.gitignore` - исключает секретные файлы из git
- ✅ `machata_bot.py` - основной файл бота

## 🌐 Деплой на Render

### 1. Настройка на Render.com

1. Создайте новый **Web Service** на Render
2. Подключите репозиторий GitHub: `https://github.com/lutifer/machata-bot`
3. Настройки:
   - **Build Command**: `pip install -r requirements.txt`
   - **Start Command**: `python machata_bot.py`
   - **Environment**: Python 3

### 2. Переменные окружения на Render

Добавьте в настройках Render следующие переменные:

```
API_TOKEN=7334437140:AAG8GYbJFcwFFj4YfpGsFKrLYBO6VlbWkRE
YOOKASSA_SHOP_ID=1231094
YOOKASSA_SECRET_KEY=live_G7u2yfiQfxt-YSwsPwG3iJsAyBz4sVENqfqvGQVosME
RENDER_EXTERNAL_URL=https://ваш-сервис.onrender.com
PORT=10000
```

⚠️ **ВАЖНО**: После деплоя Render даст вам URL вида `https://machata-bot-xxxx.onrender.com`

### 3. Настройка Webhook для Telegram

После деплоя на Render:

1. Получите URL вашего сервиса (например: `https://machata-bot-xxxx.onrender.com`)
2. Webhook URL для Telegram будет: `https://machata-bot-xxxx.onrender.com/{API_TOKEN}/`
3. Установите webhook через API Telegram или бот автоматически установит его при запуске

### 4. Настройка Webhook для ЮKassa

1. В личном кабинете ЮKassa: https://yookassa.ru/my
2. Перейдите в **"Настройки" → "HTTP-уведомления"**
3. Добавьте URL: `https://machata-bot-xxxx.onrender.com/payment`
4. Включите события:
   - ✅ `payment.succeeded`
   - ✅ `payment.waiting_for_capture`

## 🔍 Проверка работы

1. После деплоя проверьте:
   - ✅ Сервис запущен (зеленый статус на Render)
   - ✅ Логи показывают: `✅ Webhook установлен успешно`
   - ✅ Health check: `https://machata-bot-xxxx.onrender.com/` возвращает `🎵 MACHATA bot работает!`

2. Проверьте webhook Telegram:
   ```bash
   curl https://api.telegram.org/bot{API_TOKEN}/getWebhookInfo
   ```

3. Создайте тестовое бронирование в боте

## ⚠️ Возможные проблемы

### Проблема 1: Бот не отвечает
- **Причина**: Webhook не установлен или неверный URL
- **Решение**: Проверьте переменную `RENDER_EXTERNAL_URL` и логи бота

### Проблема 2: Ошибка при создании платежа
- **Причина**: Неверные ключи ЮKassa
- **Решение**: Проверьте `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY` в настройках Render

### Проблема 3: Webhook ЮKassa не работает
- **Причина**: URL недоступен или неверный
- **Решение**: Убедитесь, что URL вебхука в ЛК ЮKassa совпадает с `https://ваш-сервис.onrender.com/payment`

### Проблема 4: Ошибка при установке зависимостей
- **Причина**: Неверная версия Python или отсутствие `requirements.txt`
- **Решение**: Убедитесь, что `requirements.txt` закоммичен в репозиторий

## 📝 Команды для проверки

```bash
# Проверить статус webhook Telegram
curl https://api.telegram.org/bot{API_TOKEN}/getWebhookInfo

# Проверить health check
curl https://ваш-сервис.onrender.com/

# Проверить логи на Render
# (в панели Render → Logs)
```

## 🔐 Безопасность

⚠️ **НЕ КОММИТЬТЕ** в GitHub:
- `run_bot.command` (содержит секретные ключи)
- `machata_bookings.json` (данные пользователей)
- Любые файлы с секретными ключами

Все секреты должны быть в переменных окружения на Render!

